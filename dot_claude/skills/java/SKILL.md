---
name: Java
description: General rules for Java code generation
---

## Data Type

* For URL, use the URI class, not a string
* When a variable as a set of fixed string values, use an enum.

## Object / Builder

Use the lombok library for builder, constructor and accessors.

## Log

use the Slf4j library with the lombok @Slf4j annotation
