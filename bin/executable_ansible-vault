#!/usr/bin/env bash
# Wrapper to inject the token

args=("$@")

if [[ "${1}" == "decrypt" ]]; then
  args+=("--vault-password-file" "<(pass ansible/vault-password)")
fi

# Take the second vault
"$(which -a ansible-vault | sed -n '2p')" "${args[@]}"
