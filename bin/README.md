# Binary

This binary are here to create wrapper that overrides binary in
`.local/bin`

This is the default destination of:

* `pipx`
* or manual installation such as claude.

Other binary scripts should be created in [bin-local](../bin-local)
